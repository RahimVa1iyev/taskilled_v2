import type { InternStep } from '../types/useInternOnboarding.types'

export const INTERN_TOTAL_STEPS = 3

export const INTERN_PROGRESS: Record<InternStep, string> = {
  1: '33%',
  2: '66%',
  3: '100%',
}
